// ⚠️ TEMPORAIREMENT DÉSACTIVÉ - Configuration DB côté serveur uniquement
// La base de données sera configurée plus tard côté serveur

// Export temporaire pour éviter les erreurs
export const db = null;
export type Database = any;

// Réexport du schéma pour les types
export * from './schema';
